<UserControl x:Class="ProjectPilotLite.Wpf.Views.DeliverablesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="760">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Livrables du projet" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Livrables}"
                  SelectedItem="{Binding LivrableSelectionne}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="2*" />
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*" />
                <DataGridTextColumn Header="URL / chemin" Binding="{Binding UrlOuChemin}" Width="2*" />
                <DataGridTextColumn Header="Statut" Binding="{Binding Statut}" Width="*" />
                <DataGridTextColumn Header="Commentaire" Binding="{Binding Commentaire}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Ajouter un livrable" FontWeight="Bold" Margin="0,0,0,4" />
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBox Width="150" Margin="0,0,8,0"
                         Text="{Binding NouveauNom, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Nom" />
                <ComboBox Width="110" Margin="0,0,8,0"
                          ItemsSource="{Binding Types}"
                          SelectedItem="{Binding NouveauType}" />
                <TextBox Width="200" Margin="0,0,8,0"
                         Text="{Binding NouvelleUrl, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="URL ou chemin local" />
                <TextBox Width="150" Margin="0,0,8,0"
                         Text="{Binding NouveauCommentaire, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Commentaire" />
                <Button Content="Ajouter" Command="{Binding AjouterLivrableCommand}" Padding="10,3" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Livrable sélectionné :" VerticalAlignment="Center" Margin="0,0,8,0" />
                <Button Content="Valider" Command="{Binding ValiderCommand}" Padding="10,3" Margin="0,0,8,0" />
                <Button Content="Refuser" Command="{Binding RefuserCommand}" Padding="10,3" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>