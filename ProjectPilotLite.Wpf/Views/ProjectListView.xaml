<UserControl x:Class="ProjectPilotLite.Wpf.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:common="clr-namespace:ProjectPilotLite.Wpf.Common"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="750">
    <UserControl.Resources>
        <common:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Projets" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Rafraîchir" Command="{Binding ChargerCommand}" Padding="8,4" Margin="0,0,8,0"/>
            <TextBlock Text="Chargement..." Foreground="Gray"
                       Visibility="{Binding ChargementEnCours, Converter={StaticResource BoolToVis}}"/>
            <TextBlock Text="{Binding MessageErreur}" Foreground="Red" Margin="8,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Projets}"
                  SelectedItem="{Binding ProjetSelectionne}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="180"/>
                <DataGridTextColumn Header="Responsable" Binding="{Binding Responsable}" Width="140"/>
                <DataGridTextColumn Header="Statut" Binding="{Binding Statut}" Width="90"/>
                <DataGridTextColumn Header="Début" Binding="{Binding DateDebut, StringFormat=d}" Width="90"/>
                <DataGridTextColumn Header="Limite" Binding="{Binding DateLimite, StringFormat=d}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
