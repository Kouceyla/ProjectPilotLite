<UserControl x:Class="ProjectPilotLite.Wpf.Views.ProjectCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="420" d:DesignWidth="500">
    <Grid Margin="16">
        <StackPanel>
            <TextBlock Text="Nouveau projet" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

            <TextBlock Text="Nom *"/>
            <TextBox Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,8"/>

            <TextBlock Text="Description *"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" Height="70" TextWrapping="Wrap" Margin="0,2,0,8"/>

            <TextBlock Text="Responsable *"/>
            <TextBox Text="{Binding Responsable, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,8"/>

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Date de début"/>
                    <DatePicker SelectedDate="{Binding DateDebut}"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Date limite"/>
                    <DatePicker SelectedDate="{Binding DateLimite}"/>
                </StackPanel>
            </Grid>

            <Button Content="Créer le projet" Command="{Binding CreerCommand}"
                    Padding="8,4" HorizontalAlignment="Left"/>

            <TextBlock Text="{Binding MessageErreur}" Foreground="Red" Margin="0,8,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
